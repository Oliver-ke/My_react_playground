import styled from "react-emotion";

// Create a <Title> react component that
// renders an <h1> which is centered, plum and sized at 1.5em
export default styled("h1")`
  font-size: 1.5em;
  text-align: center;
  color: #2c2c2c;
  margin: 0.5em;
  padding: 0;
`;
